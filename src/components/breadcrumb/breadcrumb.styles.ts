import { css } from 'lit';
import componentStyles from '../../styles/component.styles';

export default css`
  ${componentStyles}

  .lynk-breadcrumb {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
  }
`;
